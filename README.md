# jiancai-rosa

捡菜市集：微信小游戏。收摊翻出好食材，用有限菜篮带走，回家卖掉或做菜。

```
npm install
npm run build
```

微信开发者工具打开本仓库根目录（`miniprogramRoot` 指向 `build/wechat/`）。日常改代码后执行 `npm run build` 或 `npm run build:wechat`。不要直接打开 `minigame/`。
